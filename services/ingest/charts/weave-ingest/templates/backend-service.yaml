{{- if .Values.backend.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "weave-ingest.fullname" . }}-backend
  labels:
    {{- include "weave-ingest.labels" . | nindent 4 }}
    app.kubernetes.io/component: backend
spec:
  type: {{ .Values.backend.service.type }}
  ports:
    - port: {{ .Values.backend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "weave-ingest.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: backend
{{- end }}
